# This workflow will upload a Python Package to PyPI when a release is created
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python#publishing-to-package-registries

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: build & push odoo-17-official-image for TOM

on:
  push:
    branches:
      - 'main/18**'
      - 'releases/18**'
      - 'rc/18**'
      - 'feature/odoo-18-tom-image'

permissions:
  contents: read

jobs:
  build-push-odoo-18-official-image:
    environment: github-build
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write

    steps:
      - name: Checkout repository and submodules
        id: checkout
        uses: actions/checkout@v2
        with:
          submodules: recursive
          token: ${{ secrets.GIT_HUB_TOKEN }}

      - id: commit
        uses: prompt/actions-commit-hash@v3


      - name: List directories
        env:
          COMMIT_HASH: ${{ steps.commit.outputs.short }}
        run: |
          ls -al .
          echo "---------- commit hash -------------"
          echo "$COMMIT_HASH"
          echo "------------------------------------"


      - name: Log in to the Container registry
        uses: docker/login-action@65b78e6e13532edd9afa3aa52ac7964289d1a9c1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GIT_HUB_TOKEN }}

      - name: Build and push Docker images
        id: push
        uses: docker/build-push-action@3b5e8027fcad23fda98b2e3ac259d8d67585f671
        with:
          context: .
          file: Dockerfile-18
          platforms: linux/amd64
          push: true
          tags: |
            ghcr.io/mikrowerk/odoo-18-official-image:latest
            ghcr.io/mikrowerk/odoo-18-official-image:${{ steps.commit.outputs.short }}
